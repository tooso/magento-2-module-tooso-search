<?php

/** @var \Tooso\SDK\Search\Result $searchData */
$searchData = $block->search->getResult();
$searchId = null;
if ($searchData !== null) {
    $searchId = $searchData->getSearchId();
}
?>

<script>
    console.debug('search id: <?=$searchId ?: 'not set'?>');
    console.debug('search data:', <?=json_encode($searchData->getResponse())?>);
</script>
